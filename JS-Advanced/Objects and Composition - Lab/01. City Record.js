function createCity(name, population, treasury) {
        const city = {
            name,
            population,
            treasury
        };
        
        console.log(city);

}

createCity('Tortuga',
    7000,
    15000
);